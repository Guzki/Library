<Window x:Class="Library.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        mc:Ignorable="d"
        Title="Simple Library App" Height="600
        " Width="800">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <GroupBox Header="Readers" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Margin="5">
                <StackPanel>
                    <ListBox x:Name="ReadersListBox" ItemsSource="{Binding Readers}" Width="300" Height="200" Margin="5" SelectionChanged="ReadersListBox_SelectionChanged"/>
                    <TextBlock Text="Reader Details:" Margin="5"/>
                    <StackPanel Margin="5">
                        <Label Content="Name:"/>
                        <TextBox x:Name="ReaderNameTextBox"/>
                    </StackPanel>
                    <TextBlock Text="Borrowed Books:" Margin="5"/>
                    <ListBox x:Name="BorrowedBooksListBox" ItemsSource="{Binding Path=SelectedReader.BorrowedBooks}" Width="300" Height="100" Margin="5" SelectionChanged="BorrowedBooksListBox_SelectionChanged"/>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Button Content="Add Reader" Click="AddReaderButton_Click" Margin="5"/>
                    <Button Content="Delete Reader" Click="DeleteReaderButton_Click" Margin="5"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Button Content="Borrow Book" Click="BorrowBookButton_Click" Margin="5"/>
                        <Button Content="Return Book" Click="ReturnBookButton_Click" Margin="5"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Books Catalog" Grid.Column="1" Grid.Row="0" Margin="5">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBox x:Name="SearchTextBox" Width="150"/>
                        <Button Content="Search" Click="SearchButton_Click" Margin="5"/>
                    </StackPanel>
                    <ListBox x:Name="BooksListBox" ItemsSource="{Binding SearchedBooks}" Height="150" Margin="5" SelectionChanged="BooksListBox_SelectionChanged"/>
                    <TextBlock Text="Book Details:" Margin="5"/>
                    <StackPanel Margin="5">
                        <Label Content="Title:"/>
                        <TextBox x:Name="BookTitleTextBox"/>
                        <Label Content="Author:"/>
                        <TextBox x:Name="BookAuthorTextBox"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Button Content="Add Book" Click="AddBookButton_Click" Margin="5"/>
                    <Button Content="Delete Book" Click="DeleteBookButton_Click" Margin="5"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
        </Grid>
    </Window>
